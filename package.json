{
  "name": "creditlens-credit-analysis",
  "version": "1.0.0",
  "private": true,
  "description": "A dependency-free, interactive credit-analysis dashboard.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test && python3 -m unittest discover -s test"
  },
  "engines": { "node": ">=20" }
}
